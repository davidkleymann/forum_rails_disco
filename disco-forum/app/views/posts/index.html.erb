<%= javascript_tag render partial: 'eventstream', formats: [:js], locals: {event_id: @event_id} %><h1>Listing posts</h1>

<table border=2>
  <thead>
    <tr>
      <th>Titel des Beitrags</th>
      <th>Text</th>
      <th>Gepostet</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.htext %></td>
        <td><%= post.time %></td>
        <td><%= post.user %></td>
        <td><%= link_to 'Ansehen', post %></td>
        <td><%= link_to 'Editieren', edit_post_path(post) %></td>
        <td><%= link_to 'Löschen', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neuer Post', new_post_path %>
