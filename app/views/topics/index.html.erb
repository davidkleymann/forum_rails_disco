<%= javascript_tag render partial: 'eventstream/eventstream', formats: [:js], locals: {event_id: @event_id} %>
<h1>Listing topics</h1>


<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Erstellt von</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= link_to topic.title, topic_path(id: topic.id) %></td>
        <td><%= topic.benutzername %></td>
        <td><%= link_to 'Edit', edit_topic_path(id: topic.id) %></td>
        <td><%= link_to 'Destroy', topic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
